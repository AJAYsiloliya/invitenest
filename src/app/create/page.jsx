import CreateClient from "./CreateClient";

export const metadata = {
  title: "Create Invitation",
  description:
    "Create and customize beautiful digital wedding, birthday, love and event invitations with InviteNest.",
};

export default function CreatePage() {
  return <CreateClient />;
}